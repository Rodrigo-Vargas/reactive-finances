{
  "name": "client",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "sourceRoot": "apps/client"
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "options": {
        "buildTarget": "client:build"
      }
    }
  }
}